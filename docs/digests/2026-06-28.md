# Dev digest — 2026-06-28

## Skills updated
- **skills.sh (`skills update -g`):** `portless` and `emulate` updated (2 skills).
- **mattpocockskills:** pulled (already current; re-linked). Recent: writing-* skills refresh, decision-mapping/glossary cleanups. No brand-new skills.
- **cursor/plugins (pstack):** NEW — [Benny issue-automation pack](https://github.com/cursor/plugins) (`reproduce-and-fix-issues`, `setup-benny`, `triage-issue-reports` skills) for automated issue reproduction/triage.
- **knowledge-work-plugins:** new `tavily` and `canva` plugins added upstream; many connector version bumps (wix, carta, valtown, exa, dropbox, zapier, auth0, langfuse…).
- **superpowers:** v6.0.1 → v6.0.3. SDD artifacts now written to working-tree `.superpowers/sdd` instead of the protected `.git/` path; Codex sync/version fixes; stopped shipping the evals submodule.
- Culls re-applied (teach/scaffold-exercises/setup-matt-pocock-skills/migrate-to-shoehorn); no broken symlinks to prune.

## Claude Code
- Installed: **2.1.187**. Latest: **2.1.195** (worth upgrading). Notable since 2.1.187:
  - `CLAUDE_CODE_DISABLE_MOUSE_CLICKS` env var; exact-match fix for hyphenated hook matchers (2.1.195).
  - `autoMode.classifyAllShell` to route all shell through the auto-mode classifier; bash-mode live path autocomplete; idle background-shell memory management (2.1.193).
  - `/rewind` can resume before a `/clear`; background agents now stop permanently; sandbox remembers approved hosts (2.1.191).
  - Skills: nested `.claude/skills` load contextually; name clashes disambiguate as `<dir>:<name>`. New `Tool(param:value)` permission syntax (e.g. `Agent(model:opus)`).
  - Agent teams: `TeamCreate`/`TeamDelete` removed — one implicit team per session under `CLAUDE_CODE_EXPERIMENTAL_AGENT_TEAMS=1`.
  - Sources: [changelog](https://code.claude.com/docs/en/changelog), [GitHub CHANGELOG](https://raw.githubusercontent.com/anthropics/claude-code/main/CHANGELOG.md)

## Research / articles
- **From Failed Trajectories to Reliable LLM Agents: Diagnosing and Repairing Harness Flaws (HarnessFix)** — trace-grounded diagnosis maps failures to a 7-layer harness taxonomy (ETCLOVG) and applies scoped repairs; +15–50% held-out. Directly relevant to making our skills/harnesses reliable. https://arxiv.org/html/2606.06324v1
- **Beyond pass@1: A Reliability Science Framework for Long-Horizon LLM Agents** — pass@1 on short tasks is blind to long-horizon reliability; frontier models hit up to 19% "meltdown" rates; capability ≠ reliability at length. Reinforces our verifiability-first ethos. https://arxiv.org/pdf/2603.29231
- **Anthropic — Effective harnesses for long-running agents** — Agent SDK compaction isn't enough; consistent progress across many context windows is still an open problem. https://www.anthropic.com/engineering/effective-harnesses-for-long-running-agents

## Tool releases
- No major releases among tracked tools in `tools.md` today (portless/emulate got skill-side updates via skills.sh, noted above).

## Candidates to act on (Linear JAS — not auto-filed)
- **Upgrade Claude Code 2.1.187 → 2.1.195** and adopt `Tool(param:value)` permission rules + nested-skill loading.
- **Try HarnessFix's ETCLOVG lens** on our own skills: when a skill fails, classify the failure by layer (Execution/Tooling/Context/Lifecycle/Observability/Verification/Governance) before patching — pairs with `linting-and-scoring`.
- **Evaluate the pstack Benny pack** for issue reproduce/triage automation.

---
Highlights: Claude Code moved to 2.1.195 (param-scoped permissions, nested-skill contextual loading, implicit agent teams). Two strong agent-reliability papers landed — HarnessFix's ETCLOVG harness taxonomy + the "beyond pass@1" long-horizon reliability framework, both appended to papers.md. Skill churn was light: portless/emulate bumped, superpowers v6.0.3 (SDD out of `.git/`), and a new pstack "Benny" issue-automation pack worth a look.
